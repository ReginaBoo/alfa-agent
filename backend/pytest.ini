# pytest.ini

[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -v --tb=short --strict-markers

markers =
    unit: Unit tests
    integration: Integration tests (require real API)
    slow: Slow tests